---
status: accepted
---

# v001 是自包含的唯一權威 Workflow Version

目前的 `strategy-forward-replication-research--v001` 保持 `draft`，並吸收仍適用的規則、門檻與說明；通過 validator、反例測試與 release checks 後才可啟用。誤植的 v011 guide 與所有失效引用將在內容遷移完成後刪除，避免 v001 依賴不存在的外部權威。
